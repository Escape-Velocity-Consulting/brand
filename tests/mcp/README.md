# MCP end-to-end tests

> **Documentation moved.** This harness is now documented in [`brand/docs/testing.md`](../../docs/testing.md) — fixture format, runner commands, report locations, how to add a test.
>
> This file is preserved as a pointer to avoid breaking external links.

## Quick reference

- Run: `npm run test:mcp` (stdio) or `npm run test:mcp:http` (HTTP)
- Fixtures: `tests/mcp/fixtures/*.json`
- Reports: `tests/mcp/report/index.html` + `tests/mcp/report-http/index.html`
- Runner: `tests/mcp/run.ts`

Full docs → [`brand/docs/testing.md`](../../docs/testing.md).
