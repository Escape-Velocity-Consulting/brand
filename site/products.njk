---
layout: base.njk
title: Products
permalink: /products/
navSlug: products
subtitle: "Templates and outputs — everything the brand produces."
---

<section>
  <div class="container">
    <div class="eyebrow">Output Types</div>
    <h2>What the Brand Produces</h2>
    <div class="nav-cards">
      <a href="{{ '/graphics/' | url }}" class="nav-card">
        <h4>Graphics</h4>
        <p>LinkedIn banners, OG images, quote cards, stats cards, social posts, and QR codes.</p>
      </a>
      <a href="{{ '/documents/' | url }}" class="nav-card">
        <h4>Documents</h4>
        <p>Letters, offers, invoices, terms of service, and reports — A4 PDFs from Markdown.</p>
      </a>
      <a href="{{ '/presentations/' | url }}" class="nav-card">
        <h4>Presentations</h4>
        <p>Slide deck templates with HTML viewer, PDF export, and per-slide PNG output.</p>
      </a>
    </div>
  </div>
</section>

<section>
  <div class="container">
    <div class="eyebrow">Generate</div>
    <h2>Common Commands</h2>
    <table>
      <thead>
        <tr><th>Output</th><th>Command</th></tr>
      </thead>
      <tbody>
        <tr><td>Document PDF</td><td><code>npm run pdf -- content.md --type letter -o letter.pdf</code></td></tr>
        <tr><td>Social image</td><td><code>npm run image -- --input templates/social/linkedin-banner.html --preset linkedin-banner -o banner.png</code></td></tr>
        <tr><td>Slide deck</td><td><code>npm run pres -- deck.md -o previews/decks/my-deck --pdf --png</code></td></tr>
      </tbody>
    </table>
    <p class="footnote" style="margin-top: 24px;">
      Or ask Claude — the <a href="{{ '/tooling/' | url }}">escape-velocity-brand skill</a> handles all output types via the MCP server.
    </p>
  </div>
</section>
